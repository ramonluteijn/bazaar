<footer class="w-full h-[50px] bg-amber-400">
    <div class="container mx-auto h-full">
        <div class="row p-3 text-center h-full">
            <p>&copy; {{date('Y')}} <a href="{{route('home.index')}}">{{config('app.name')}}</a></p>
        </div>
    </div>
</footer>
